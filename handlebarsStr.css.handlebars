{{#sprites}}
.{{name}} {  
  background-image: url(../sprites.png);
  background-position: {{px.offset_x}} {{px.offset_y}};
  width: {{px.width}};
  height: {{px.height}};
  display: block;
}
.{{name}}() {  
  background-image: url(../sprites.png);
  background-position: {{px.offset_x}} {{px.offset_y}};
  width: {{px.width}};
  height: {{px.height}};
  display: block;
}
{{/sprites}}